# Task List

## Done

- Create Nx workspace.
- Add Next.js web app.
- Add Express API app.
- Add health route.
- Add environment examples.
- Add documentation structure.
- Add Neo4j package and connection config.
- Use Neo4j driver lifecycle during API bootstrap.
- Add graph constraint/index setup.
- Add people search, create, detail, and update API foundation.
- Add relationship create, list, and delete API foundation.
- Add inverse relationship pair creation.
- Add relationship validation for duplicates, self-links, parent limits, and family cycles.
- Add graph network endpoint for the frontend.
- Wire frontend initial graph load, person creation, and relationship creation to the API.
- Refactor frontend into modular component-based architecture.
- Implement relationship editing by `relationshipGroupId` (UI + API).
- Replace local-only delete and merge UI behavior with robust API-backed flows.
- Implement Merge Review Queue with dry-run impact previews.
- Implement Audit/Activity Logging (Track graph changes).
- Implement Consistency Verification Jobs (Phase 4).
- Implement Role-Based Access Control (RBAC) (Phase 5).
- Implement Idempotency Engine (X-Idempotency-Key) (Phase 5).
- Implement Production Metrics & Admin Dashboard API (Phase 5).
# Task List

## Done

- Create Nx workspace.
- Add Next.js web app.
- Add Express API app.
- Add health route.
- Add environment examples.
- Add documentation structure.
- Add Neo4j package and connection config.
- Use Neo4j driver lifecycle during API bootstrap.
- Add graph constraint/index setup.
- Add people search, create, detail, and update API foundation.
- Add relationship create, list, and delete API foundation.
- Add inverse relationship pair creation.
- Add relationship validation for duplicates, self-links, parent limits, and family cycles.
- Add graph network endpoint for the frontend.
- Wire frontend initial graph load, person creation, and relationship creation to the API.
- Refactor frontend into modular component-based architecture.
- Implement relationship editing by `relationshipGroupId` (UI + API).
- Replace local-only delete and merge UI behavior with robust API-backed flows.
- Implement Merge Review Queue with dry-run impact previews.
- Implement Audit/Activity Logging (Track graph changes).
- Implement Consistency Verification Jobs (Phase 4).
- Implement Role-Based Access Control (RBAC) (Phase 5).
- Implement Idempotency Engine (X-Idempotency-Key) (Phase 5).
- Implement Production Metrics & Admin Dashboard API (Phase 5).

## Next

- Add Force-Directed layout for GraphCanvas.
- Add first product workflow (e.g., Member Onboarding).

## Later
 
- API tests.
- CI workflow.
- Deployment documentation.
